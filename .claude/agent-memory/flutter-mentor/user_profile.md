---
name: user-profile
description: User's Flutter experience level and working style preferences
metadata:
  type: user
---

The user writes their own code — never edit files directly, always present suggestions as copy-pasteable snippets.

The user is building a strength-training app (structure) with Flutter + Rust (flutter_rust_bridge). They appear to have working Flutter knowledge (Riverpod, ConsumerWidget, bridge calls) but benefit from step-by-step guidance on architectural decisions (navigation patterns, layout strategies).

They prefer concrete recommendations with explicit tradeoffs over exhaustive option lists.
