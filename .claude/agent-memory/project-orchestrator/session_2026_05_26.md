---
name: session_2026_05_26
description: Session 2026-05-26 — completed microcycle_count feature and training_programs_screen UI improvements
metadata:
  type: project
---

## Completed this session

Branch: `frontend-create-mesocycle-builder-screen`

### UI improvements — `training_programs_screen.dart`
- Fixed rectangular hover bug: replaced `Card` + `ListTile` with `Ink` + `InkWell` + matching `borderRadius`
- Added depth via `BoxDecoration` with `boxShadow`, border, and `secondaryContainer` fill on selection
- Added `_ModeBadge` (Algorithmic / Manual) and `_WeekCountBadge` (N weeks) to each list item

### `microcycle_count` feature — committed and verified
- **Architecture decision:** derive count via LEFT JOIN + COUNT(mc.id) in persistence layer; do not store on domain type; reason: duration is dynamic (user can extend/shorten mesocycles)
- `MesocycleRow` added to `backend/src/persistence/mesocycles.rs` — persistence-internal query result type carrying id, name, mode, microcycle_count
- `get_mesocycle` and `list_mesocycles` updated with LEFT JOIN query
- `microcycle_count: pub(crate) u32` added to `MesocycleDTO`; `From<&Mesocycle>` impl dropped; API constructs DTO explicitly
- FRB bridge regenerated; all tests passing
- `_WeekCountBadge` widget added in Flutter; `flutter analyze` clean

## Next task

Build the mesocycle builder screen. There is a `//TODO: navigate to builder screen` comment in `_TrainingProgramsScreenState._createTrainingProgram()`. The builder screen does not yet exist. This is the primary goal of the current branch (`frontend-create-mesocycle-builder-screen`).

## User workflow preferences
- User writes all code themselves — specialists provide exact snippets to type
- Specialist responses forwarded verbatim
- Consult software-architect before any implementation with design ambiguity
- Rust before Flutter when frontend depends on backend changes
