---
name: user-profile
description: User role, background, and Rust skill level inferred from the structure project
metadata:
  type: user
---

User is building a strength-training app called `structure` with a Rust backend and Flutter frontend. They are comfortable with:
- Domain modeling with private fields and `pub(crate)` constructors
- Persistence layer patterns (rusqlite, query_map, row mapping)
- Idiomatic Result propagation with typed error enums
- The distinction between domain types and DTOs

They work at a level where they understand ownership semantics and can apply idiomatic patterns once pointed at them. They want to be guided, not have code written for them — they implement snippets themselves.

See [[feedback-read-only-mentor]] for collaboration style.
