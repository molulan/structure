[package]
name = "structure-core"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
rusqlite = {version = "0.39.0", features = ["bundled"] }
thiserror = "2.0.18"
